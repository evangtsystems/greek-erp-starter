DATABASE_URL="postgresql://erp:erp@localhost:5432/erp?schema=public"
REDIS_URL="redis://localhost:6379"
PORT=4000

# Wrapp staging only. Never commit real values.
WRAPP_BASE_URL="https://staging.wrapp.ai/api/v1"
WRAPP_PARTNER_API_KEY=""
# Original partner staging account, used only for direct staging tests.
WRAPP_STAGING_TENANT_EMAIL=""
WRAPP_STAGING_TENANT_API_KEY=""
WRAPP_WEBHOOK_ENDPOINT=""
# Generate once with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
WRAPP_CREDENTIALS_ENCRYPTION_KEY=""

# Required by protected ERP administration endpoints.
# Generate once with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ERP_ADMIN_API_KEY=""
